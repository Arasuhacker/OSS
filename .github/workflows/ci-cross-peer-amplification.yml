name: isolated-chia-cross-peer-amplification

on:
  push:
    branches: [ci-rlv3-6f4a9d]
    paths-ignore: ['ci/results/**']
  workflow_dispatch:

permissions:
  contents: write

jobs:
  amplification:
    strategy:
      fail-fast: false
      matrix:
        target:
          - name: main-pinned
            ref: d61ed07dee7e9f846b475184db29e885f4699d2a
          - name: release-2.7.3
            ref: release/2.7.3
    runs-on: ubuntu-24.04
    timeout-minutes: 100
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ci-rlv3-6f4a9d
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
      - name: Install target
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends libgmp-dev libssl-dev pkg-config
          git clone --filter=blob:none https://github.com/Chia-Network/chia-blockchain.git target
          git -C target checkout '${{ matrix.target.ref }}'
          git -C target rev-parse HEAD > TARGET_COMMIT.txt
          python -m pip install --upgrade pip setuptools wheel
          python -m pip install -e './target[dev]'
      - name: Execute amplified race
        id: campaign
        continue-on-error: true
        env:
          PYTHONPATH: ${{ github.workspace }}/target
          PYTHONUNBUFFERED: '1'
        run: |
          set -o pipefail
          python ci/cross_peer_tx_race_amplification.py 2>&1 | tee AMPLIFICATION.log
          echo "exit_code=${PIPESTATUS[0]}" >> "$GITHUB_OUTPUT"
      - name: Stage result
        if: always()
        run: |
          mkdir -p amplification-results
          {
            echo "target=${{ matrix.target.name }}"
            echo "ref=${{ matrix.target.ref }}"
            echo "commit=$(cat TARGET_COMMIT.txt 2>/dev/null)"
            echo "exit_code=${{ steps.campaign.outputs.exit_code }}"
            grep -E 'EVIDENCE=|BASELINE_HONEST_CLOSED=|ATTACK_HONEST_CLOSED=|ATTACKER_CLOSED_EVENTS=|ATTACK_VALID_TXS_IN_POOL=|CONTROL_HONEST_CLOSED=|CONTROL_ATTACKER_CLOSED_EVENTS=|RESULT=|Traceback|ERROR' AMPLIFICATION.log 2>/dev/null | tail -n 320 || true
          } > amplification-results/${{ matrix.target.name }}.txt
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: cross-peer-amplification-${{ matrix.target.name }}
          path: amplification-results/${{ matrix.target.name }}.txt
          retention-days: 7

  publish:
    needs: amplification
    if: always()
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ci-rlv3-6f4a9d
      - uses: actions/download-artifact@v4
        with:
          pattern: cross-peer-amplification-*
          path: collected
          merge-multiple: true
      - name: Commit matrix
        run: |
          mkdir -p ci/results
          cat collected/*.txt > ci/results/cross-peer-amplification-matrix.txt
          git config user.name github-actions
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add ci/results/cross-peer-amplification-matrix.txt
          if ! git diff --cached --quiet; then
            git commit -m 'Record cross-peer amplification matrix [skip ci]'
            git push origin HEAD:ci-rlv3-6f4a9d
          fi
