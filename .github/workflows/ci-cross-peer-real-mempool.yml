name: isolated-chia-cross-peer-real-mempool

on:
  push:
    branches: [ci-rlv3-6f4a9d]
    paths-ignore: ['ci/results/**']
  workflow_dispatch:

permissions:
  contents: write

jobs:
  real-mempool-race:
    strategy:
      fail-fast: false
      matrix:
        target:
          - name: main-pinned
            ref: d61ed07dee7e9f846b475184db29e885f4699d2a
          - name: release-2.7.3
            ref: release/2.7.3
    runs-on: ubuntu-24.04
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ci-rlv3-6f4a9d
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
      - name: Install exact target
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends libgmp-dev libssl-dev pkg-config
          git clone --filter=blob:none https://github.com/Chia-Network/chia-blockchain.git target
          git -C target checkout '${{ matrix.target.ref }}'
          git -C target rev-parse HEAD > TARGET_COMMIT.txt
          python -m pip install --upgrade pip setuptools wheel
          python -m pip install -e './target[dev]'
      - name: Execute real mempool race
        id: campaign
        continue-on-error: true
        env:
          PYTHONPATH: ${{ github.workspace }}/target
          PYTHONUNBUFFERED: '1'
        run: |
          set -o pipefail
          python ci/cross_peer_tx_race_real_mempool.py 2>&1 | tee REAL_RACE.log
          echo "exit_code=${PIPESTATUS[0]}" >> "$GITHUB_OUTPUT"
      - name: Stage exact result
        if: always()
        run: |
          mkdir -p real-race-results
          {
            echo "target=${{ matrix.target.name }}"
            echo "ref=${{ matrix.target.ref }}"
            echo "commit=$(cat TARGET_COMMIT.txt 2>/dev/null)"
            echo "exit_code=${{ steps.campaign.outputs.exit_code }}"
            grep -E 'PENDING_ATTRIBUTE=|PENDING_TYPE=|SOURCE_PEER_BOUND=|EVIDENCE=|NEGATIVE_CONTROL|REQUESTED_FROM_A|B_ACCEPTED|VALID_TX_IN_MEMPOOL_AFTER_B|A_CLOSED|CONTROL=|CONTROL_PASS=|RESULT=|Traceback|ERROR' REAL_RACE.log 2>/dev/null | tail -n 320 || true
            echo '--- tail ---'
            tail -n 260 REAL_RACE.log 2>/dev/null || true
          } > real-race-results/${{ matrix.target.name }}.txt
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: cross-peer-real-mempool-${{ matrix.target.name }}
          path: real-race-results/${{ matrix.target.name }}.txt
          retention-days: 7

  publish:
    needs: real-mempool-race
    if: always()
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ci-rlv3-6f4a9d
      - uses: actions/download-artifact@v4
        with:
          pattern: cross-peer-real-mempool-*
          path: collected
          merge-multiple: true
      - name: Commit matrix result
        run: |
          mkdir -p ci/results
          cat collected/*.txt > ci/results/cross-peer-real-mempool-matrix.txt
          git config user.name github-actions
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add ci/results/cross-peer-real-mempool-matrix.txt
          if ! git diff --cached --quiet; then
            git commit -m 'Record real-mempool cross-peer race matrix [skip ci]'
            git push origin HEAD:ci-rlv3-6f4a9d
          fi
